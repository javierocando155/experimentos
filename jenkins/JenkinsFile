node('jenkins-slave') {
    
     stage('test pipeline') {
        sh(script: """
            echo "hello"
           git credentialsId: 'jejejej', url: 'git@github.com:javierocando155/prueba.git', branch: 'main'
           cd ./golang
           
           docker build -t test .
        """)
    }
}