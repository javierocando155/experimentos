node('jenkins-slave') {
    
     stage('git') {
        git credentialsId: 'jejejej', url: 'git@github.com:javierocando155/prueba.git', branch: 'main'
        sh(script: """
            echo "hello"
           cd ./golang
           
           docker build -t test .
        """)
    }
}