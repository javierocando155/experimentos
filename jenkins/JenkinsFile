node('jenkins-slave') {
    
    stage('Git') {
        git credentialsId: 'jejejej', url: 'git@github.com:javierocando155/prueba.git', branch: 'main'
        sh(script: """
            echo "hello"
           cd ./golang
           
           docker build -t test .
        """)
    }
    stage('Build Image') {

        sh(script: """
            echo "hello"
           cd ./golang
           
           docker build -t test .
        """)
    }
}