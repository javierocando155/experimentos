node('jenkins-slave') {
    
     stage('test pipeline') {
        sh(script: """
            echo "hello"
           git branch: 'main', credentialsId: 'jejejej', url: 'git@github.com:javierocando155/prueba.git'
           cd ./golang
           
           docker build -t test .
        """)
    }
}